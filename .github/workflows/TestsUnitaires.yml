name: Tests Unitaires Avant Push
run-name: ${{ github.actor }} is testing 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt install phpunit php-gd php-sqlite3 php-xdebug
      - run: echo "💡 Tests Unitaires ..."
      - run: /vendor/bin/phpunit tst
