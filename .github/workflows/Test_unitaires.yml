name: Tests Unitaires
run-name: ${{ github.actor }} is testing 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Tests Unitaires"
      - run: apt-get install phpunit php-gd php-sqlite3 php-xdebug
      - run: sudo make test
